Quickstart

This handbook is written in [mdbook](https://rust-lang.github.io/mdBook/).

```sh
# run in project root to start locally
mdbook serve
```

To update the handbook, change the files in src/, commit the change, and push to main branch to deploy.

